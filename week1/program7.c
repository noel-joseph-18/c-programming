#include <stdio.h>
int main ()
{
    float a, b,t ;
    int n;
     printf("Enter the two numbers : \n",a,b);
     scanf("%f%f",&a,&b);

	t=a*b;n=a*b;
	printf("product of the numbers is (as integer)  %d ", n);
	printf("product of the numbers is (as floating point number) %f ", t);
	return 0;
	
}
