#include <stdio.h>
int main () {
	int a,b,d ;
	printf("the 3 numbers are : ", a, b,d );
	scanf("%d%d%d",&a,&b,&d);
	if (a==b && a==d)
	{
		printf("equal");
			}
	else  (a==b!=d && a!=b==d && a!=b!=d) ;
	{
		printf("not equal ");
}
	return 0;
}
